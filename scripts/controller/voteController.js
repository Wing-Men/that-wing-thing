'use strict';

(function(module){
  var voteController = {};

  voteController.updateVotes = function() {
    //this function will update the votes of a given resturuant in webDB once we have a sql table for locations
  };
  module.voteController = voteController;
})(window);
